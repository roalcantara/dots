CMD+RIGHT -> End of the line
CMD+LEFT -> Beginning of the line

CMD+ALT+d -> Toggle Document Diagnostics
CMD+SHIFT+d -> Toggle Workspace Diagnostics
CMD+ALT+l -> Toggle Loclist
CMD+SHIFT+l -> Toggle Quickfix list
CMD+ALT+r -> Toggle References

CMD+ALT+K -> Toggle Hover Doc
CMD+CTRL+d -> Toggle definitions

CMD+CTRL+H -> Toggle Signature Help
CMD+CTRL+r -> Toggle Rename
CMD+CTRL+a -> Toggle Code Actions
CMD+CTRL+l -> Toggle Code Lens

`:g/println/d`          -> Delete all lines that contain the string "print"
`:g/println/norm I// `  -> Append "//" to each line contain the string "println"


i -> Insert mode before the cursor
a -> Insert mode after the cursor
I -> Insert mode before the cursor and enter insert mode

If enabled for "v" (Visual mode) then
  double-click -> selects word-wise
  triple-click -> makes it line-wise
  quadruple-click -> makes it rectangular block-wise.

Left-click anywhere in a text buffer to place the cursor there.

To delete a from the current cursor position another position using click:
  -> type |d| then left-click to delete text from the current cursor position to the position where you clicked.
