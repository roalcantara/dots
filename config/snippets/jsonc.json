{
  "Add tsconfig": {
    "prefix": "tsconfig",
    "body": [
      "{",
      "  \"extends\": \"@tsconfig/recommended/tsconfig.json\",",
      "  \"compilerOptions\": {",
      "    \"esModuleInterop\": true,",
      "    \"strictPropertyInitialization\": false,",
      "    \"noImplicitReturns\": true,",
      "    \"resolveJsonModule\": true,",
      "    \"outDir\": \"dist\",",
      "    \"baseUrl\": \".\",",
      "    \"paths\": {}",
      "  },",
      "  \"exclude\": [\"node_modules\", \"dist\", \"tmp\"]",
      "}"
    ],
    "description": "Add tsconfig"
  },
  "Add tsconfig.base.json": {
    "prefix": "tsconfig:base",
    "body": [
      "{",
      "  \"extends\": \"@tsconfig/recommended/tsconfig.json\",  ",
      "  \"compileOnSave\": false,",
      "  \"compilerOptions\": {",
      "    \"rootDir\": \".\",",
      "    \"sourceMap\": true,",
      "    \"declaration\": false,",
      "    \"esModuleInterop\": true,",
      "    \"strictPropertyInitialization\": false,",
      "    \"noImplicitReturns\": false,    ",
      "    \"baseUrl\": \".\",",
      "    \"paths\": {}",
      "  },",
      "  \"exclude\": [\"node_modules\", \"tmp\", \"dist\"]",
      "}"
    ],
    "description": "Add tsconfig.base.json"
  },
  "Kotlin VSCode settings.json": {
    "prefix": [
      "kotlin",
      "kotlin:settings"
    ],
    "body": [
      "\"kotlin.languageServer.path\": \"/usr/local/bin/kotlin-language-server\",",
      "\"java.configuration.updateBuildConfiguration\": \"automatic\",",
      "\"java.test.config\": [",
      "  {",
      "    \"name\": \"JavaTestConfig\",",
      "    \"workingDirectory\": \"${workspaceFolder}\",",
      "    \"args\": [\"-c\", \"br.com\"],",
      "    \"vmargs\": [\"-Xmx512M\"],",
      "    \"env\": { \"key\": \"value\" }",
      "  }",
      "],"
    ],
    "description": "Kotlin VSCode settings.json"
  },
  "ESLint VSCode settings.json": {
    "prefix": [
      "eslint",
      "eslint:settings"
    ],
    "body": [
      "  \"[javascript][typescript][typescriptreact][javascriptreact]\": {",
      "    \"editor.codeActionsOnSave\": {",
      "      \"source.fixAll.eslint\": true",
      "    }",
      "  },",
      "  \"eslint.format.enable\": true,",
      "  \"eslint.run\": \"onSave\",",
      "  \"eslint.validate\": [",
      "    \"javascript\",",
      "    \"typescript\",",
      "    \"typescriptreact\",",
      "    \"javascriptreact\"",
      "  ],",
      "  \"eslint.codeAction.showDocumentation\": {",
      "    \"enable\": true",
      "  },",
      "  \"eslint.workingDirectories\": [",
      "    {",
      "      \"mode\": \"auto\"",
      "    }",
      "  ],"
    ],
    "description": "ESLint VSCode settings.json"
  },
  ".eslintrc.json": {
    "prefix": [
      "eslintrc"
    ],
    "body": [
      "{",
      "  \"root\": true,",
      "  \"extends\": [\"eslint:recommended\", \"plugin:eslint-plugin/recommended\", \"plugin:node/recommended\"],",
      "  \"env\": {",
      "    \"node\": true",
      "  },",
      "  \"ignorePatterns\": [\"dist/\"],",
      "  \"rules\": {",
      "    \"prefer-arrow-callback\": 1,",
      "    \"arrow-body-style\": [\"error\", \"as-needed\", { \"requireReturnForObjectLiteral\": false }],",
      "    \"camelcase\": \"error\",",
      "    \"curly\": [\"error\", \"multi\"],",
      "    \"func-style\": [\"error\", \"expression\"],",
      "    \"no-lonely-if\": \"error\",",
      "    \"no-unneeded-ternary\": \"error\",",
      "    \"prefer-const\": \"error\",",
      "    \"prefer-object-spread\": \"error\",",
      "    \"prefer-promise-reject-errors\": [\"error\", { \"allowEmptyReject\": true }],",
      "    \"object-shorthand\": \"error\",",
      "    \"no-magic-numbers\": [\"error\", { \"ignoreDefaultValues\": true, \"ignore\": [-1, 0, 1] }],",
      "    \"no-extra-label\": \"error\",",
      "    \"no-console\": 0,",
      "    \"consistent-return\": 0,",
      "    \"object-curly-newline\": 0,",
      "    \"lines-between-class-members\": 0,",
      "    \"implicit-arrow-linebreak\": 0,",
      "    \"quotes\": [",
      "      \"error\",",
      "      \"single\",",
      "      {",
      "        \"avoidEscape\": true,",
      "        \"allowTemplateLiterals\": true",
      "      }",
      "    ]",
      "  }",
      "}",
      ""
    ],
    "description": ".eslintrc.json"
  },
  "Raycast .eslintrc.json": {
    "prefix": [
      "eslintrc:raycast"
    ],
    "body": [
      "{",
      "  \"root\": true,",
      "  \"extends\": [\"@raycast\"],",
      "  \"rules\": {",
      "    \"prefer-arrow-callback\": 1,",
      "    \"arrow-body-style\": [\"error\", \"as-needed\", { \"requireReturnForObjectLiteral\": false }],",
      "    \"camelcase\": \"error\",",
      "    \"curly\": [\"error\", \"multi\"],",
      "    \"func-style\": [\"error\", \"expression\"],",
      "    \"no-lonely-if\": \"error\",",
      "    \"no-unneeded-ternary\": \"error\",",
      "    \"prefer-const\": \"error\",",
      "    \"prefer-object-spread\": \"error\",",
      "    \"prefer-promise-reject-errors\": [\"error\", { \"allowEmptyReject\": true }],",
      "    \"object-shorthand\": \"error\",",
      "    \"no-magic-numbers\": [\"error\", { \"ignoreDefaultValues\": true, \"ignore\": [-1, 0, 1] }],",
      "    \"no-extra-label\": \"error\",",
      "    \"no-console\": 0,",
      "    \"consistent-return\": 0,",
      "    \"object-curly-newline\": 0,",
      "    \"lines-between-class-members\": 0,",
      "    \"implicit-arrow-linebreak\": 0",
      "  }",
      "}",
      ""
    ],
    "description": "Raycast .eslintrc.json"
  },
  "luals.json": {
    "prefix": [
      "luals"
    ],
    "body": [
      "return {",
      "  addonManager = {",
      "    -- Whether the addon manager is enabled or not.",
      "    enable = true,",
      "",
      "    -- Specifies the git branch used by the addon manager.",
      "    repositoryBranch = '',",
      "",
      "    -- Specifies the git path used by the addon manager.",
      "    repositoryPath = ''",
      "  },",
      "  codeLens = {",
      "    -- Enable code lens.",
      "    enable = false,",
      "  },",
      "  completion = {",
      "    -- When the input looks like a file name, automatically require this file.",
      "    autoRequire = true,",
      "",
      "    -- Shows function call snippets.",
      "    callSnippet = 'Disable',",
      "",
      "    -- Previewing the relevant code snippet of the suggestion may help you understand the usage of the suggestion. The number set indicates the number of intercepted lines in the code fragment. If it is set to 0, this feature can be disabled.",
      "    displayContext = 0,",
      "",
      "    -- Enable completion.",
      "    enable = true,",
      "",
      "    -- Shows keyword syntax snippets.",
      "    keywordSnippet = 'Replace',",
      "",
      "    -- The symbol used to trigger the postfix suggestion.",
      "    postfix = '@',",
      "",
      "    -- The separator used when require.",
      "    requireSeparator = '.',",
      "",
      "    -- Display parameters in completion list. When the function has multiple definitions, they will be displayed separately.",
      "    showParams = true,",
      "",
      "    -- Show contextual words in suggestions.",
      "    showWord = 'Fallback',",
      "",
      "    -- Whether the displayed context word contains the content of other files in the workspace.",
      "    workspaceWord = true,",
      "  },",
      "  diagnostics = {",
      "    -- Disabled diagnostic (Use code in hover brackets).",
      "    disable = {},",
      "",
      "    -- Do not diagnose Lua files that use the following scheme.",
      "    disableScheme = {",
      "      'git',",
      "    },",
      "",
      "    -- Enable diagnostics.",
      "    enable = true,",
      "",
      "    -- Defined global variables.",
      "    globals = {},",
      "",
      "    -- Find defined global variables using regex.",
      "    globalsRegex = {},",
      "",
      "    -- Modify the diagnostic needed file status in a group. \"Opened = only diagnose opened files\"",
      "    -- Any = diagnose all files",
      "    -- None = disable this diagnostic",
      "    -- Fallback means that diagnostics in this group are controlled by diagnostics.neededFileStatus separately. Other settings will override individual settings without end of !.",
      "    groupFileStatus = {},",
      "",
      "    -- Modify the diagnostic severity in a group. Fallback means that diagnostics in this group are controlled by diagnostics.severity separately. Other settings will override individual settings without end of !.",
      "    groupSeverity = {},",
      "",
      "    -- How to diagnose ignored files.",
      "    ignoredFiles = 'Opened',",
      "",
      "    -- How to diagnose files loaded via workspace.library.",
      "    -- Opened = only diagnose opened files",
      "    libraryFiles = 'Opened',",
      "",
      "    -- Modify the diagnostic severity.",
      "    -- Any = diagnose all files",
      "    -- None = disable this diagnostic",
      "    -- End with ! means override the group setting diagnostics.groupFileStatus.",
      "    severity = {},",
      "",
      "    -- Do not diagnose unused-local when the variable name matches the following pattern.",
      "    unusedLocalExclude = {},",
      "",
      "    -- Latency (milliseconds) for workspace diagnostics.",
      "    workspaceDelay = 3000,",
      "",
      "    -- Set the time to trigger workspace diagnostics.",
      "    workspaceEvent = 'OnSave',",
      "",
      "    -- Workspace diagnostics run rate (%). Decreasing this value reduces CPU usage, but also reduces the speed of workspace diagnostics. The diagnosis of the file you are currently editing is always done at full speed and is not affected by this setting.",
      "    workspaceRate = 100",
      "  },",
      "  doc = {",
      "    -- Treat specific field names as package, e.g. m_* means XXX.m_id and XXX.m_type are package, witch can only be accessed in the file where the definition is located.",
      "    packageName = {},",
      "",
      "    -- Treat specific field names as private, e.g. m_* means XXX.m_id and XXX.m_type are private, witch can only be accessed in the class where the definition is located.",
      "    privateName = {},",
      "",
      "    -- Treat specific field names as protected, e.g. m_* means XXX.m_id and XXX.m_type are protected, witch can only be accessed in the class where the definition is located and its subclasses.",
      "    protectedName = {},",
      "",
      "    -- TODO = Needs documentation",
      "    regengine = 'glob',",
      "",
      "    -- TODO = Needs documentation",
      "    docScriptPath = ''",
      "  },",
      "  format = {",
      "    -- The default format configuration. Has a lower priority than .editorconfig file in the workspace. Read formatter docs to learn usage.",
      "    defaultConfig = {},",
      "",
      "    -- Enable code formatter.",
      "    enable = true,",
      "  },",
      "  hint = {",
      "    -- Show hints of array index when constructing a table.",
      "    arrayIndex = 'Auto',",
      "",
      "    -- If the called function is marked ---@async, prompt await at the call.",
      "    await = true,",
      "",
      "    -- Enable the propagation of await. When a function calls a function marked ---@async,it will be automatically marked as ---@async.",
      "    awaitPropagate = false,",
      "",
      "    -- Enable inlay hint.",
      "    enable = false,",
      "",
      "    -- Show hints of parameter name at the function call.",
      "    paramName = 'All',",
      "",
      "    -- Show type hints at the parameter of the function.",
      "    paramType = true,",
      "",
      "    -- If there is no semicolon at the end of the statement, display a virtual semicolon.",
      "    semicolon = 'SameLine',",
      "",
      "    -- Show hints of type at assignment operation.",
      "    setType = false,",
      "  },",
      "  hover = {",
      "    -- Enable hover.",
      "    enable = true,",
      "",
      "    -- When the value corresponds to multiple types, limit the number of types displaying.",
      "    enumsLimit = 5,",
      "",
      "    -- Whether to expand the alias. For example, expands ---@alias myType boolean|number appears as boolean|number, otherwise it appears as `myType'.",
      "    expandAlias = true,",
      "",
      "    -- When hovering to view a table, limits the maximum number of previews for fields.",
      "    previewFields = 10,",
      "",
      "    -- Hover to view numeric content (only if literal is not decimal).",
      "    viewNumber = true,",
      "",
      "    -- Hover to view the contents of a string (only if the literal contains an escape character).",
      "    viewString = true,",
      "",
      "    -- The maximum length of a hover to view the contents of a string.",
      "    viewStringMax = 1000,",
      "  },",
      "  language = {",
      "    -- (VSCode only) Automatically insert \"---@ \" after a line break following a annotation.",
      "    completeAnnotation = true,",
      "",
      "    -- (VSCode only) Fix incorrect auto-indentation, such as incorrect indentation when line breaks occur within a string containing the word \"function.\"",
      "    fixIndent = true,",
      "  },",
      "  misc = {",
      "    -- Specify the executable path in VSCode.",
      "    executablePath = '',",
      "",
      "    -- Command line parameters when starting the language server in VSCode.",
      "    parameters = {},",
      "  },",
      "  nameStyle = {",
      "    -- Set name style config",
      "    config = {},",
      "  },",
      "  runtime = {",
      "    -- Adjust the enabled state of the built-in library. You can disable (or redefine) the non-existent library according to the actual runtime environment.",
      "    builtin = 'default',",
      "",
      "    -- File encoding. The ansi option is only available under the Windows platform.",
      "    fileEncoding = 'utf8',",
      "",
      "    -- Format of the directory name of the meta files.",
      "    meta = '${version} ${language} ${encoding}',",
      "",
      "    -- Supports non-standard symbols. Make sure that your runtime environment supports these symbols.",
      "    nonstandardSymbol = {},",
      "",
      "    -- When using require, how to find the file based on the input name. Setting this config to ?/init.lua means that when you enter require 'myfile', ${workspace}/myfile/init.lua will be searched from the loaded files. if runtime.pathStrict is false, ${workspace}/**/myfile/init.lua will also be searched. If you want to load files outside the workspace, you need to set workspace.library first.",
      "    path = {",
      "      '?.lua',",
      "      '?/init.lua'",
      "    },",
      "",
      "    -- When enabled, runtime.path will only search the first level of directories, see the description of runtime.path.",
      "    pathStrict = false,",
      "",
      "    -- Plugin path. Please read wiki to learn more.",
      "    plugin = '',",
      "",
      "    -- Additional arguments for the plugin.",
      "    pluginArgs = {},",
      "",
      "    -- The custom global variables are regarded as some special built-in variables, and the language server will provide special support The following example shows that 'include' is treated as' require '.",
      "    special = {",
      "      include = 'require'",
      "    },",
      "",
      "    -- Allows Unicode characters in name.",
      "    unicodeName = false,",
      "",
      "    -- Lua runtime version.",
      "    version = 'Lua 5.4',",
      "  },",
      "  semantic = {",
      "    -- Semantic coloring of type annotations.",
      "    annotation = true,",
      "",
      "    -- Enable semantic color. You may need to set editor.semanticHighlighting.enabled to true to take effect.",
      "    enable = true,",
      "",
      "    -- Semantic coloring of keywords/literals/operators. You only need to enable this feature if your editor cannot do syntax coloring.",
      "    keyword = false,",
      "",
      "    -- Semantic coloring of variables/fields/parameters.",
      "    variable = true,",
      "  },",
      "  signatureHelp = {",
      "    -- Enable signature help.",
      "    enable = true,",
      "  },",
      "  spell = {",
      "    -- Custom words for spell checking.",
      "    dict = {},",
      "  },",
      "  type = {",
      "    -- Allowed to assign the number type to the integer type.",
      "    castNumberToInteger = true,",
      "",
      "    -- Strictly check the shape of the table.",
      "    checkTableShape = false,",
      "",
      "    -- When a parameter type is not annotated, it is inferred from the function's call sites.",
      "    inferParamType = false,",
      "",
      "    -- Once one subtype of a union type meets the condition, the union type also meets the condition.",
      "    weakUnionCheck = false,",
      "  },",
      "  typeFormat = {",
      "    -- Configures the formatting behavior while typing Lua code.",
      "    config = {}",
      "  },",
      "  window = {",
      "    -- Show progress bar in status bar.",
      "    progressBar = true,",
      "",
      "    -- Show extension status in status bar.",
      "    statusBar = true,",
      "  },",
      "  workspace = {",
      "    -- Automatic detection and adaptation of third-party libraries, currently supported libraries are = OpenResty, Cocos4.0, LÖVE, LÖVR, skynet, Jass",
      "    checkThirdParty = '',",
      "",
      "    -- Ignored files and directories (Use .gitignore grammar).",
      "    ignoreDir = {",
      "      '.vscode'",
      "    },",
      "",
      "    -- Ignore submodules.",
      "    ignoreSubmodules = true,",
      "",
      "    -- In addition to the current workspace, which directories will load files from. The files in these directories will be treated as externally provided code libraries, and some features (such as renaming fields) will not modify these files.",
      "    library = {},",
      "",
      "    -- Max preloaded files.",
      "    maxPreload = 5000,",
      "",
      "    -- Skip files larger than this value (KB) when preloading.",
      "    preloadFileSize = 500,",
      "",
      "    -- Ignore files list in .gitignore .",
      "    useGitIgnore = true,",
      "",
      "    -- Add private third-party library configuration file paths here, please refer to the built-in configuration file path",
      "    userThirdParty = {}",
      "  }",
      "}",
      ""
    ],
    "description": "Lua Language Server Settings with default values"
  },
  "Biome.jsonc": {
    "prefix": "biome",
    "body": [
      "{",
      "  \"$schema\": \"https://biomejs.dev/schemas/2.0.5/schema.json\",",
      "  \"formatter\": {",
      "    \"enabled\": true",
      "  },",
      "  \"linter\": {",
      "    \"enabled\": true",
      "  },",
      "  \"assist\": {",
      "    \"enabled\": true",
      "  }",
      "}"
    ],
    "description": "Add biome configuration file"
  }
}