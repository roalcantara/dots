return {
  au = require('core/vi/au'), -- Autocommands
  buffers = require('core/vi/buffers'), -- Buffer and editor utilities
  commands = require('core/vi/commands'), -- Command and keymap utilities
  lsp = require('core/vi/lsp'), -- LSP utilities
  paths = require('core/vi/paths'), -- Path and file system utilities
  plugins = require('core/vi/plugins'), -- Plugin management utilities
  strings = require('core/vi/strings'), -- String manipulation utilities
  tables = require('core/vi/tables'), -- Table manipulation utilities
}
